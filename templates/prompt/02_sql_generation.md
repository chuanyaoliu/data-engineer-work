根据字段映射与口径，生成可运行的 Spark SQL（Hive/Delta 任选其一）。
要求：
- 使用 CTE 分步，关键步骤添加注释
- 明确去重主键与策略（ROW_NUMBER / DISTINCT / 聚合）
- 处理空值、异常值、时区与分区过滤
- 输出主键唯一性校验 SQL 与核心校验指标

输出：
1) 建表语句（如需要）
2) 核心查询 SQL（最终产出）
3) 校验 SQL（唯一性 / 行数 / 空值率等）


