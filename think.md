
## 数据开发提效

agent

mcp

理解需求，编码测试逻辑准确，mock数据测试验收



git remote add origin https://github.com/chuanyaoliu/data-engineer-work.git

### 方案一

大数据数仓开发，包括需求拆解，写spark sql自测调试，测试验证，数据监控 这四个环节，我想节约ai的能力做提效，你有什么好的方法

#### 总体思路
- 利用模板化提示+结构化产物，固定输入输出，减少反复沟通。
- 把可自动化的部分落成脚本/Notebook，AI负责生成初稿与复核。
- 每个环节都输出“可执行物”：需求工单、SQL草案+自测数据、用例清单、监控规则/告警。

#### 提示词
- Cursor连接与上下文注入
  - “你将作为数据工程助理。我会提供需求说明、表结构、历史口径与样例数据。请严格按我给的模板输出产物，不要发挥。若信息不足先列出澄清问题，再给出可执行的最小草案。”
- Agent协作（拆解→生成→校验）
  - “分三步：1) 拆解与澄清；2) 生成产物(表格/SQL/断言)；3) 校验与改进建议。每步结束输出‘完成/待补充项清单’。”
- MCP/知识库检索增强
  - “先检索知识库中相似口径与历史SQL，列出差异点与潜在冲突，再在此基础上生成当前方案。”
- 串接提示（需求→SQL→测试→监控）
  - “输入以下需求与字段映射：… 请依次输出：A) 名词口径表与澄清点；B) Spark SQL草案(含CTE/去重/边界处理)与10条mock数据；C) 回归用例表与断言SQL；D) 监控规则(行数/空值率/环比偏差)与告警分级。所有输出均以可复制执行为准。”

##### 可直接复制的模板
- 需求澄清与拆解
  - “以下是业务需求/口径/限制条件：<粘贴>。请输出：1) 澄清问题清单；2) 业务名词与口径表(字段/定义/示例)；3) 事实/维度与粒度；4) 来源表与字段映射；5) 初步指标与计算口径；6) DAG任务列表(任务名/依赖/频率/负责人)。不足处先列澄清后给最小可行方案。”
- SQL 生成（Spark/Hive/Delta 任选其一）
  - “基于以下字段映射与口径，生成可运行的Spark SQL：要求包含CTE分步注释、去重策略、空值与边界处理、时间分区与时区说明、主键唯一性校验SQL。<粘贴表结构/映射/分区规则> 输出：1) 建表语句(如需)；2) 核心查询SQL；3) 校验SQL。”
- Mock 数据与自测
  - “为上述SQL生成10条覆盖边界的mock数据(含正常/空值/重复/晚到/跨月)，给出insert语句与期望输出表样例(表格)。随后给出Notebook步骤：创建临时表→插入样例→运行SQL→对比断言→导出报告。”
- 断言与回归用例
  - “为该任务输出测试用例表：用例ID、场景、构造方式、输入、期望、断言SQL/阈值、前置依赖。确保覆盖空值率、去重率、主键唯一、分区row_count、同比/环比边界。”
- 监控与告警
  - “为目标表/指标生成监控配置：1) 调度SLA；2) 完整性(row_count、null_rate、distinct_pk)；3) 质量阈值(上下限/同比/环比波动)；4) 血缘守护(上游schema变更)。输出监控SQL或规则配置，并给出预警/严重的阈值与接收人。”
- 异常诊断与修复建议
  - “基于以下异常样例/时序数据/告警日志，生成根因假设Top5及验证步骤，给出临时绕过与长期修复建议，并标注对口径影响范围。<粘贴日志/指标序列>”

#### 1) 需求拆解（需求→模型→任务）
- 目标：把口述/文档需求转成标准化产物：领域名词表、口径定义、血缘与调度清单。
- 提示模板（粘贴需求后直用）：
  - “请基于以下需求，输出：1) 业务名词表(字段/含义/口径/示例)；2) 事实/维度划分与粒度；3) 来源表与字段映射；4) 初步指标口径；5) DAG任务列表(依赖/频率/负责人)。要求表格化，歧义列出澄清点。”
- 产物形态：
  - 名词口径表.md、来源映射.xlsx、DAG任务清单.csv、澄清问题.md
- 自动化建议：
  - 用脚本读取历史指标口径库，AI做相似口径比对，自动标红冲突。

#### 2) Spark SQL 开发与自测调试
- 目标：从“口径+映射”自动生成SQL草案和自测样例，快速迭代。
- 提示模板：
  - “根据字段映射与口径，生成可运行的Spark SQL（支持Hive/Delta），包含：CTE分步、关键步骤注释、边界处理(null/重复/时区)、主键/唯一性校验SQL。附上10条可插入的mock数据及期望输出。”
- 自测清单（最少覆盖）：
  - 空值/脏数据、去重逻辑、口径边界(含当月首日/跨月/节假日)、时区/晚到、主键唯一性
- 自动化建议：
  - Notebook单键运行：生成mock表→插入样例→跑SQL→校验断言→导出对比报告。
  - 本地小样本/采样器：从源表按条件采样N=1000做快速跑数。

#### 3) 测试与验收（可回归）
- 目标：把自测变成可回归用例，形成机器可判定的PASS/FAIL。
- 提示模板：
  - “为下述SQL/口径产出测试用例表：用例编号、场景、构造方式、输入样例、期望输出、校验断言(SQL/指标阈值)、依赖前置。标注边界与异常用例。”
- 落地方式：
  - 生成“用例表.csv+断言SQL清单.sql”，通过CI任务跑断言，出JUnit/HTML报告。
  - 回填质检看板：用例覆盖率、最近失败率、平均修复时长。

#### 4) 数据监控与告警
- 目标：覆盖“产出是否到达、值是否合理、趋势是否异常、口径变更是否影响”。
- 监控模板清单：
  - 调度SLA：T+1/T+0达成率
  - 完整性：分区row_count、主键唯一性、空值率、去重率
  - 质量阈值：指标上下限、同比/环比偏差、波动点检测
  - 血缘守护：上游Schema变更/字段缺失的触发告警
- 提示模板：
  - “根据下列表结构与指标，生成监控规则SQL/配置：维度、阈值、窗口、告警接收人、分级策略(预警/严重)。附带自动修复建议。”
- 自动化建议：
  - 用AI从历史指标序列生成自适应阈值和异常说明，告警时附上可读诊断与最可能根因。

#### 工具与集成建议
- AI接口：Cursor/本地模型+函数调用，用固定模板驱动。
- Dev形态：PySpark/SQL Notebook模板+CLI一键：init需求→gen SQL→gen mock→run→report。
- CI/CD：拉取“用例+断言”，跑小样本回归；生产跑全量后执行质量断言。
- 知识库：沉淀口径、映射、常见范式与风格规则，供AI检索增强(RAG)。

#### 最小闭环落地（1周）
- D1：固化4个提示模板与文档产物结构
- D2：SQL草案生成+mock数据自动跑通(采样源)
- D3：断言SQL产出与CI集成出报告
- D4：3类基础监控规则上线(row_count/空值率/环比偏差)
- D5：回顾与模板迭代，纳入知识库

#### 参考实现
- 方案总览：`docs/solution/AI_assisted_data_engineering_workflow.md`
- 提示模板：`templates/prompt/`
- CLI 脚手架：`scripts/cli/ai_de_helper.py`
- 使用指引：`workflows/runbook.md`



